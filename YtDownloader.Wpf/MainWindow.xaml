<Window x:Class="YtDownloader.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YouTube下载器" Height="250" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="视频URL:"/>
            <TextBox x:Name="UrlTextBox" Height="25"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="保存文件名:"/>
            <TextBox x:Name="FileNameTextBox" Height="25"/>
        </StackPanel>

        <Button Grid.Row="2" x:Name="DownloadButton" 
                Content="下载" Height="35" Click="DownloadButton_Click"/>

        <TextBlock Grid.Row="3" x:Name="StatusText" 
                   Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>