<Window x:Class="YtDownloader.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="m3u8下载器" Height="400" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="视频URL:" FontWeight="Bold"/>
            <TextBox x:Name="UrlTextBox" Height="28" Margin="0,5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="保存文件名（不带后缀）:" FontWeight="Bold"/>
            <TextBox x:Name="FileNameTextBox" Height="28" Margin="0,5"/>
        </StackPanel>

        <Button Grid.Row="2" x:Name="DownloadButton" 
                Content="开始下载" Height="40" FontSize="16" Click="DownloadButton_Click"/>

        <!-- 显示完整命令 -->
        <StackPanel Grid.Row="3" Margin="0,15,0,5">
            <TextBlock Text="完整命令（可复制）:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="CommandTextBox" 
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="110"
                     Background="#F0F0F0"
                     FontFamily="Consolas"/>
        </StackPanel>

        <TextBlock Grid.Row="4" x:Name="StatusText" 
                   Margin="0,10,0,0" TextWrapping="Wrap" Foreground="Green"/>
    </Grid>
</Window>